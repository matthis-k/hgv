package kit.pse.hgv.controller.commandController.commands;

/**
 * This class handles all commands that manage the extension or the commands
 * from the extention
 */
public abstract class ExtensionCommand extends Command {
}